import React from 'react';

const Menu: React.FC = () => {
    return (
        <nav>
            <ul>
                {/* Add menu items here */}
            </ul>
        </nav>
    );
};

export default Menu;